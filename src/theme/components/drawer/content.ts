import { DrawerContent } from "@mantine/core";

export default DrawerContent.extend({
  defaultProps: {
    bg: "dark.9",
    bd: "1 solid slate.9",
    // style: {
    //   borderColor: "var(--paper-border-color)",
    // },
  },
});
